import {
    StyleSheet,
} from 'react-native';
const ViewStyle = StyleSheet.create({
    type1: {
        fontSize: 10,
        borderColor:'green',
        borderWidth:2,
        height:60,
    }
});
export {ViewStyle};